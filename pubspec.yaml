name: todo_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  get_it: ^7.6.0
  hive_flutter: ^1.1.0
  hive_generator: ^2.0.0
  uuid: ^3.0.7
  timeago: ^3.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.4

flutter:
  uses-material-design: true
